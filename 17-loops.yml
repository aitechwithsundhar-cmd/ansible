- name: install packages
  hosts: localhost
  connection: local 
  tasks:
  - name: install packages
    ansible.builtin.packages:
    name: "{{item}}"
    state: present
    loop: 
    - mysql
    - nginx
    - zip
